Centered dialog overlay for creating/editing an activity or category.

```jsx
<Modal open={open} title="Nova atividade" onClose={close} footer={<Button onClick={save}>Salvar</Button>}>
  <Input label="Nome" />
</Modal>
```
