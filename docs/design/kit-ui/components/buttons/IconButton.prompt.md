Icon-only round button for toolbars, table rows, and modal close controls. Always pass `ariaLabel`.

```jsx
<IconButton icon="edit" ariaLabel="Editar" />
<IconButton icon="trash" variant="danger" ariaLabel="Excluir" />
```
