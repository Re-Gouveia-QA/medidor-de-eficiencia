Single-select radio option, grouped by `name`, for mutually-exclusive choices like report period.

```jsx
<Radio name="periodo" label="Este mes" checked={p==='mes'} onChange={() => setP('mes')} />
```
