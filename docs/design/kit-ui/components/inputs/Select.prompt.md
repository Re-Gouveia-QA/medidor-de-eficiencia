Dropdown select with the same sketchy field shell as Input, used for choosing a category, period, or other fixed set of options.

```jsx
<Select label="Categoria" options={[{value:'estudo',label:'Estudo'},{value:'lazer',label:'Lazer'}]} />
```
