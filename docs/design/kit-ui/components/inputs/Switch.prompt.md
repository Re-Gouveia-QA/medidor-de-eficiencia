On/off toggle switch, used for settings like the dark-theme switch or a "possui valor" category flag.

```jsx
<Switch label="Tema escuro" checked={dark} onChange={e => setDark(e.target.checked)} />
```
