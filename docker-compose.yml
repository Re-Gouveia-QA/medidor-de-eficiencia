services:
  db:
    image: postgres:16
    container_name: medidor-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: medidor_eficiencia
    ports:
      - "5433:5432"
    volumes:
      - medidor_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d medidor_eficiencia"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  medidor_db_data:
